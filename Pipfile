[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
ruff = "*"
pytest = "*"

[requires]
python_version = "3.12"

[scripts]
test = "pytest"
tidy = "bash -c 'ruff check --fix && ruff format'"
